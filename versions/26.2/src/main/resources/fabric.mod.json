{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "${name}",
  "description": "Adds some helpful enchantments for Shulker Boxes (MC 26.2 port - WORK IN PROGRESS: core enchantment registry + container storage ported, compat mixins/renderers/config/screens not yet ported, see the port-26.2 branch commit history and Mod.kt)",
  "authors": [
    "RubixDev"
  ],
  "contact": {
    "homepage": "https://github.com/RubixDev/EnchantedShulkers",
    "sources": "https://github.com/RubixDev/EnchantedShulkers",
    "issues": "https://github.com/RubixDev/EnchantedShulkers/issues"
  },

  "license": "GPL-3.0-only",

  "environment": "*",
  "entrypoints": {
    "main": [{
      "value": "de.rubixdev.enchantedshulkers.Mod",
      "adapter": "kotlin"
    }]
  },
  "mixins": [
    "enchantedshulkers.mixins.json"
  ],
  "accessWidener": "${id}.accesswidener",

  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-api": "*",
    "minecraft": "26.2",
    "fabric-language-kotlin": ">=${fabric_kotlin_version}"
  }
}
